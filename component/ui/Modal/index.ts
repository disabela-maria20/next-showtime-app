'use client'

import ModelBody from './Body'
import ModelContent from './Content'
import ModelFooter from './Footer'
import ModelRoot from './Root'
import ModelTitle from './Title'

export const Modal = {
  Footer: ModelFooter,
  Title: ModelTitle,
  Content: ModelContent,
  Root: ModelRoot,
  Body: ModelBody
}
